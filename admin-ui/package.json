{
  "name": "ego-admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "esbuild-svelte": "^0.9.5",
    "svelte": "^5.56.6"
  }
}
