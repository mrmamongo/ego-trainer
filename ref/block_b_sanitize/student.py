#!/usr/bin/env python3
"""
student.py — ТВОЙ файл. Пиши решения здесь.

Как работать:
    1. Удали `pass` и напиши код
    2. Запускай проверку:  python checker.py
    3. Смотри результат: ✅ (зачёт) или ❌ (не зачёт)

Правила:
    - Не меняй НАЗВАНИЯ функций
    - Не меняй АРГУМЕНТЫ функций
    - return результат, не print
"""


def task_b_sanitize(payload):
    """
    Задача B: Рекурсивная санитизация email'ов

    У тебя есть вложенный словарь — ответ от агента.
    Внутри могут быть email-адреса на ЛЮБОМ уровне вложенности.
    Нужно найти все email'ы и заменить их на строку "<REDACTED>".

    Аргументы:
        payload — вложенный словарь (dict), который может содержать:
                  - строки (str)
                  - числа (int, float)
                  - списки (list)
                  - другие словари (dict)

    Верни:
        Новый словарь (или изменённый оригинальный), где ЛЮБАЯ строка,
        содержащая символ '@' и точку '.' (email-лайк), заменена на "<REDACTED>".

    Правила:
        - Проверяй ТОЛЬКО строки (type(x) == str)
        - Строка считается email, если в ней ЕСТЬ '@' И '.'
        - Списки и словари обходи рекурсивно
        - Числа и другие типы не трогай
        - Функция должна работать на ЛЮБОЙ глубине вложенности

    Пример:
        Вход:  {"name": "Ivan", "email": "ivan@test.com", "count": 42}
        Выход: {"name": "Ivan", "email": "<REDACTED>", "count": 42}

    Подсказка:
        Проверка на email: '@' in text and '.' in text
        Рекурсия: вызывай саму себя для вложенных dict и list.
    """
    pass
